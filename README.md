# statxplorerplus

Default base URL: https://stat-xplore.dwp.gov.uk/webapi/rest/v1

```r
# install.packages("remotes")
remotes::install_local("statxplorerplus_0.3.0.zip")
library(statxplorerplus)

# Only API key is required now; base URL defaults automatically
set_statxplore_config(api_key = "YOUR_API_KEY")
```
