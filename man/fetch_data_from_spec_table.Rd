% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_data_from_spec_table}
\alias{fetch_data_from_spec_table}
\title{Fetch data from a tidy "spec table"}
\usage{
fetch_data_from_spec_table(
  spec_table,
  timeout = 600,
  attempts = 10,
  only_keep_data = TRUE
)
}
\arguments{
\item{spec_table}{A tibble/data frame with (at minimum) \code{database_id},
\code{measure_id}, \code{field_id}, and \code{value_id}. A \code{value_group} column may be
present for grouped recodes.}

\item{timeout}{Number of seconds to wait before timing out on each attempt.}

\item{attempts}{Maximum number of attempts (only retries on 504 responses).}

\item{only_keep_data}{Passed to \code{\link[=fetch_table]{fetch_table()}}.}
}
\value{
Either a single data frame (default) or a full results list
(when \code{only_keep_data = FALSE}).
}
\description{
Convenience wrapper that:
\enumerate{
\item converts a spec table (as produced by \code{\link[=convert_json_to_spec_table]{convert_json_to_spec_table()}}) into
a Stat-Xplore JSON spec,
\item writes it to a temporary \code{.json} file, and
\item calls \code{\link[=fetch_table]{fetch_table()}} to download and parse the results.
}
}
\details{
Grouped recodes are supported via a \code{value_group} column. Where \code{value_group}
is present, the function constructs the \code{custom} mapping needed by
\code{\link[=extract_results]{extract_results()}} so returned item labels align with the grouped query.
}
