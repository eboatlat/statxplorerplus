% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_table}
\alias{fetch_table}
\title{Send a table query and return tidy results}
\usage{
fetch_table(
  query,
  filename = NULL,
  custom = NULL,
  timeout = 600,
  attempts = 10,
  only_keep_data = TRUE,
  drop_total_rows = FALSE
)
}
\arguments{
\item{query}{Stat-Xplore query as a JSON string.}

\item{filename}{Path to a text file containing a Stat-Xplore query. If
supplied, \code{query} is ignored.}

\item{custom}{Named list of character vectors giving item labels to use for
selected fields when constructing the results.}

\item{timeout}{Number of seconds to wait before timing out.}

\item{attempts}{Maximum number of attempts (only retries on 504 responses).}

\item{only_keep_data}{If \code{TRUE}, return just the first results data frame
(useful when the query has a single measure). If \code{FALSE}, return the full
results list produced by \code{\link[=extract_results]{extract_results()}}.}

\item{drop_total_rows}{If \code{TRUE}, rewrite the query to set all \code{total} flags
to \code{FALSE} before sending (this drops total rows generated by Stat-Xplore).}
}
\value{
Either a results list with elements \code{measures}, \code{fields}, \code{items},
\code{uris}, and \code{dfs}, or (when \code{only_keep_data = TRUE}) a single tibble/data
frame.
}
\description{
Submits a query to the Stat-Xplore \code{table} endpoint and converts the returned
cube(s) to a list containing metadata and one long-form data frame per
measure.
}
\details{
You can pass the query as a JSON string in \code{query}, or read it from a file
path in \code{filename} (which takes priority). This is often convenient when
exporting queries from the Stat-Xplore website "table builder".

If your query uses custom aggregate variables, you may need to override item
labels using \code{custom}, because the returned metadata may not match the
dimensions implied by the query.
}
